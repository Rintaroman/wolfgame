<h3>ユーザー作成画面</h3>
<%= form_with model:@player, url: players_path, local: true do |f| %>
  <div class="new-player">
    <label class="form-text">名前を入力してください</label>
    <%= f.text_field :name, class:"input-text" %>
    <%= f.submit "登録" %>
  </div>
<% end %> 